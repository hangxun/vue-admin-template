// 请求url
export const baseUrlObj = {
  development: '//localhost:3000',
  production: '//localhost:3000',
  test: '//localhost:3000'
}

// 是否开启线上模式
export const isOnline = true

// 导航栏, ''(空)/matched(面包屑导航)/tabs(标签页导航), 标签页会使用页面缓存
export const navigate = 'tabs'
